# References

Store detailed, task-specific documentation here.

Keep each reference focused and link to it from `SKILL.md` with an explicit
condition, for example:

```markdown
Read `references/api-errors.md` when an API request returns a non-success
status.
```

Avoid chains of references. Files in this directory should be readable directly
from `SKILL.md`.
